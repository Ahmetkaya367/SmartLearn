server:
  port: 8080

spring:
  ai:
    google:
      genai:
        api-key: ${SPRING_AI_GEMINI_API_KEY:YOUR_GEMINI_API_KEY}
        chat:
          options:
            model: gemini-1.5-flash

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
